# Blank.blue

## Pre-requisite

On debian/ubuntu:

```shell
make install-ubuntu
```

Other distributions [install hugo](https://gohugo.io/installation/) at the version `0.81.0`.


## Building and serving the site

### Build only

```shell
make build
```

### Build and serve all at once

```shell
make serve
```